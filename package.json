{
  "name": "react.dev",
  "version": "0.0.0",
  "main": "/src/index.js",
  "homepage": "https://xandergardner.github.io/etf_portfolio_composition",
  "scripts": {
    "start": "npm run copy-assets && react-scripts start",
    "build": "npm run copy-assets && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build",
    "copy-assets": "ncp public/assets/ build/assets/"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "ncp": "^2.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
